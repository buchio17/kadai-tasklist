<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : 内容 : <%= task.content %> / ステータス: <%= task.status %></li>
  <% end %>
</ul>

<%= link_to '新規タスクの作成', new_task_path %>